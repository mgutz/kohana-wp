<?php

interface IModel {
    static function nu($attributes);
    function create($attributes)
    function create_x($attributes);
    function
    function 
}
   *  new(attributes)
    * create(attributes)
    * create!(attributes)
    * find(id_or_array)
    * destroy(id_or_array)
    * destroy_all
    * delete(id_or_array)
    * delete_all
    * update(ids, updates)
    * update_all(updates)
    * exists?
